interface:
  display_name: "Soundgrid"
  short_description: "Repo-specific patterns and workflows for soundgrid"
  default_prompt: "Use the soundgrid repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true